# Tests

The test suite for the annotation processor lives in a separate repository and is shared across several related projects. The `test/` directory is a Git submodule that points to that repository.
